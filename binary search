/*b.s.l.ramya awdc rjy assignment-1*/
#include <stdio.h>

int main()
{
    int i,n,count=0,a[10];
    printf("enter no of elements");
    scanf("%d",&n);
    for(i=0;i,n;i++)
    scanf("%d",&a[i]);
    for(i=0;i<n;i++)
    {
        if(a[i]>0)
        {
            a[count++]=a[i];
        }
    }
      while(count>n)
      {
          a[count++]=0;
      }
      for(i=0;i<n;i++)
      {
      printf("%d",a[i]);  
      }
    
    return 0;
}
